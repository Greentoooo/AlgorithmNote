假如我们需要计算一个数的二进制中非0位的个数，可以利用 n&(n-1) 的特性  
例如：  
`9= 0b1001;`  
`9 & 8 = 0b1000;`  
`0b1000 & 0b0111 = ob0000 = 0;`  
可以看到经历两次计算后，9已经变成了0，而2正是9的二进制表示中1的个数

```C++
// 整数二进制中 1 的个数
int n;
cin>>n;
int count = 0;
while(n)
{
    ++ count;
    n = (n-1) & n; 
}
```